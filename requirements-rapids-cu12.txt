# Optional Linux CUDA 12 RAPIDS dependencies.
# Install these only on a RAPIDS-compatible server; keep them out of the main
# requirements file to avoid resolver conflicts on Windows or CPU machines.
--extra-index-url=https://pypi.nvidia.com
cudf-cu12==24.10.*
cugraph-cu12==24.10.*
cupy-cuda12x>=13.0,<14.0
